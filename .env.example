# Copy to .env and fill in. Never commit .env.
# Only VITE_-prefixed values reach the browser bundle — keep secrets unprefixed.

# Public (browser-safe)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server only — no VITE_ prefix
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
