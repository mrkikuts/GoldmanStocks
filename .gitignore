# Secrets — never commit real keys (.env.example holds the variable names only)
.env
.env.*
!.env.example

# Dependencies
node_modules

# Build output
dist
.output
.nitro
.vinxi
.tanstack
.wrangler
.vercel

# OS / editor
.DS_Store
*.log

# A second copy of the whole app, exported from Lovable and used as the source for the
# design merge. Not part of the build — delete it once you're happy with the merge.
lovable-new-frontend/
